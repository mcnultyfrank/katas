// 15. How do you find all permutations of a string?

// ABC, ACB, BAC, BCA, CAB, CBA


    var findAllPermutations = (exampleString: String) => {
        let split = exampleString.split("");
        // if (split.length <= 1){
        //     throw "type more than 2 letters to find permutations";
        // }
        // try {
            // for (let i = 0; i < split.length; i++) {
            //     console.log(split[i]);
            // }
            return 'a';
    //     } catch (error) {
    //         console.error(error);
    //     }

    // }
    }
    findAllPermutations('abc');
    
    


export = {};  