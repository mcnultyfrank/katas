// Letters Shared between Two Words
// Create a function that returns the number of characters shared between two words.

// Examples
// sharedLetters("apple", "meaty") ➞ 2
// // Since "ea" is shared between "apple" and "meaty".
// sharedLetters("fan", "forsook") ➞ 1
// sharedLetters("spout", "shout") ➞ 4

    const sharedLetters = (firstString, secondString) => {
        splitFirstString = firstString.split("").sort();
        splitSecondString = secondString.split("").sort();
        for (let i = 0; i < splitFirstString.length; i++) {
                
        
            
        }


    }

    sharedLetters("spout", "shout")